<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.Parcial1amacn4avfiordaliso"
        tools:targetApi="31">

        <!-- Configurar LoginActivity como pantalla de inicio -->
        <activity android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- RegisterActivity (para registro de usuario) -->
        <activity android:name=".RegisterActivity"
            android:exported="false" />

        <!-- MainActivity solo accesible después del login -->
        <activity android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name" />
        <activity android:name=".EditarMovimientoActivity" />
        <activity android:name=".EditarCuentaActivity" />

    </application>
</manifest>